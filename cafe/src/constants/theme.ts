import { Colors, Spacing, BorderRadius, Shadows } from "./colors";

export const THEME = {
  colors: Colors,
  spacing: Spacing,
  borderRadius: BorderRadius,
  shadows: Shadows,
};

export default THEME;
